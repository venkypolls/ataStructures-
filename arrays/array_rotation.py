#testing input
import os
import sys

arr = input("enter the array to be rotated")
n = eval(input(("enter the number of places by which it should rotate left")))
numarr = [int(i) for i in arr]
print(numarr,n)

